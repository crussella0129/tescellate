# Sprint 4 Integration Tests

The per-lattice widget pattern is already exercised by hex and square
(sprints 0 and 2). Sprint 4 instantiates the same generic at a third
coord type — the round-trip + render-dispatch surface is identical, so
the unit-test round-trip from T-404 is the load-bearing integration
check. No additional harness added.
