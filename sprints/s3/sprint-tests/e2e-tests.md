# Sprint 3 End-to-End Tests

**Status:** N/A. Engine-only sprint; no user-facing surface lands.

The reference-examples tests double as E2E for the Carbide language
itself: they parse + evaluate real Carbide expressions against the
engine, which is the highest-fidelity test we have at this layer.
Manual UI verification not required.
