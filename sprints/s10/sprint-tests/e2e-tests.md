# Sprint 10 e2e-tests

Viewport culling. 4 new `GridMetrics` range tests (full / scrolled / boundary-straddle / empty-axis); 254/254 UI tests pass; workspace green. Scroll-render correctness + perf verified manually in a release build. CI green; PR #184 merged as `b18ede1`.
