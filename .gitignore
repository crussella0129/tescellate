# Rust
/target/
**/target/
Cargo.lock.bak
*.rs.bk
*.pdb

# Native formula compile cache
.tescellate-cache/
.carbide-cache/

# Node / Electron / Vite
node_modules/
**/node_modules/
dist/
dist-electron/
.vite/
out/
*.tsbuildinfo

# Python (for the PyO3 venv during dev)
__pycache__/
*.pyc
.venv/
venv/

# IDE / editor
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs / coverage
*.log
coverage/
.nyc_output/

# Local workbooks for testing (dual-extension per ADR-015 back-compat)
*.tscl
!examples/*.tscl
*.crbd
!examples/*.crbd

# OS junk
desktop.ini

# >>> sprint-loops >>>
# Ephemeral sprint working memory — regenerable; the real outcome lives in the
# per-task git commits + decisions.md. KEEP tracked (long-term memory the
# protocol depends on): decisions.md, agent-tasks/, confidence.txt.
sprints/
*.tmp
# <<< sprint-loops <<<
