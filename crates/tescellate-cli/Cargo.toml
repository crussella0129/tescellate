[package]
name = "tescellate-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Headless CLI for the Tescellate core. Placeholder in v161 (no headless commands yet); kept as a structural entry point for future eval / validate / batch work."

[[bin]]
name = "tescellate-core"
path = "src/main.rs"
